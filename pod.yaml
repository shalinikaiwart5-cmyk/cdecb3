apiversion: v1
kind: pod
metadata:
  name: mypod
  label:
     app: mypod
spec:
   containers:
     - name: my-conatainer
       image: nginx:latest
       ports:
         - conatinerPort: 80
           name: http
           protocol: tcp
       resources:
         requests:
           memory: "64Mi"
           cpu: "150m"

apiversion: v1
kind: Service
metadata:
  name: clusertservice
  label:
    app: clusertservice
spec:
 type: clusterIP
 selector: 
  app: mypod
ports:
 - port: 80
   #targetport: 80
  name: nginx
  protocol: TCP
